# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CleanSwift(VIP)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CleanSwift(VIP)

  target 'CleanSwift(VIP)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CleanSwift(VIP)UITests' do
    # Pods for testing
  end

end
